# Backend API base URL (do NOT include the /api prefix here)
API_BASE_URL=http://127.0.0.1:9000

# Optional. Leave empty for local dev (push and tipping run in mock/offline mode).
ONESIGNAL_APP_ID=
REVENUECAT_API_KEY=
