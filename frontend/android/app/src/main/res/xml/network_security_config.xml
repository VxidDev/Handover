<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: no cleartext anywhere. Base enforces HTTPS for all hosts. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Debug builds keep cleartext for emulators — see src/debug/res/xml/network_security_config.xml override -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    <!-- Production domains — explicitly pinned, cleartext false (redundant with base-config, documents intent) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">fvlabs.org</domain>
        <domain includeSubdomains="true">api.fvlabs.org</domain>
        <domain includeSubdomains="true">api-handover.fvlabs.org</domain>
    </domain-config>
</network-security-config>
